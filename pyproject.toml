[project]
name = "mobilanalyse-marked"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "marimo>=0.23.3",
  "matplotlib>=3.8.0",
  "pyarrow>=16.0.0",
  "polars>=1.0.0",
]

[tool.uv]
package = false
